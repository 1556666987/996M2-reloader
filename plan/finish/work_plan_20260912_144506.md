# 工作计划：窗口布局记录与自动恢复

## 目标

为自动重载工具增加 M2Server 主窗口与客户端控制台窗口的布局记录、清除和自动恢复功能，并保持现有配置字段不丢失。

## 已确认信息

- 范围：仅处理 M2Server 主窗口和 `E:\龙龙火龙七改\client\game.exe` 控制台窗口，不处理标题为“局域网17300”的游戏窗口。
- 当前状态：项目已有 Tkinter 面板、M2Server 菜单重载、Ctrl+S 触发、托盘和 config.json 持久化；窗口布局功能尚未实现。
- 默认布局：采用当前检测到的两个目标窗口布局。
- 配置策略：有自定义记录时优先使用自定义记录；无自定义记录时使用默认布局；记录和清除不能覆盖字体、重载选择等其他配置。
- 记录要求：点击“记录窗口”时两个目标窗口必须同时存在且可定位，否则提示并中止写入。
- 计划文件：`plan/work_plan.md`；完成后归档到 `plan/finish/`。
- 最后更新时间：2026-09-12 00:45（Asia/Shanghai）

## 详细工作计划

- [x] 1. 检查现有代码与当前窗口状态，确定布局字段和默认值。
- [x] 2. 实现窗口识别、布局记录/清除、配置合并读写及面板按钮。
- [x] 3. 在 Ctrl+S 流程中恢复布局，并处理窗口缺失、最小化和越界情况。
- [x] 4. 更新文档/变更记录，执行语法和可运行性验证。
- [x] 5. 完成后将计划归档到 `plan/finish/`。

## 已经完成

- 已确认目标窗口为 M2Server 主窗口与客户端控制台窗口。
- 已读取当前窗口状态：M2Server 位于 DISPLAY2，`(-760, 5, 754×516)`；客户端控制台位于 DISPLAY2，`(-765, 517, 759×519)`。
- 已创建 `plan/` 与 `plan/finish/` 目录。
- 已确认 GitHub 访问通过 `127.0.0.1:7897` 代理，并将约定写入项目级 `AGENTS.md`。
- 已在面板增加“记录窗口”和“清除记录”按钮。
- 已增加 `window_layout` 配置字段，并采用当前实测布局作为默认值；读写时保留其他配置字段。
- 已实现 M2Server 与客户端控制台窗口识别、记录、清除和 Ctrl+S 前布局恢复。
- 已修复修改字体时覆盖 `selected_items` 的配置写入问题。
- 已更新 README、DEV_SUMMARY 和 CHANGELOG，补充布局功能及当前实现说明。
- `python -m py_compile 自动重载.pyw` 通过，`git diff --check` 无空白错误；默认布局、配置回退和坐标边界逻辑离线验证通过。
- 已增加窗口枚举异常隔离，并验证当前运行中的两个目标窗口可被识别、布局读取结果与默认值一致。

## 待确认问题 / 阻塞项

- 暂无。

## 下一步

计划已完成，已归档至 `plan/finish/`。

## 验收标准

- 面板存在“记录窗口”和“清除记录”按钮。
- 两个目标窗口同时存在时可记录屏幕、坐标和尺寸；任一缺失时提示且不写入记录。
- `config.json` 保存布局时保留其他字段。
- Ctrl+S 时自定义布局优先，否则使用默认布局。
- 清除记录后恢复使用默认布局。
- 代码通过 `py_compile`，文档与实际实现一致。

## 更新记录

- [2026-09-12 00:00] 创建计划，确认范围、默认布局、配置策略和归档路径。
- [2026-09-12 00:05] 完成现状检查，确定 `window_layout` 配置字段与当前 DISPLAY2 默认布局。
- [2026-09-12 00:20] 完成布局功能实现：按钮、配置合并、窗口记录/清除及 Ctrl+S 恢复。
- [2026-09-12 00:35] 完成文档、语法检查和离线验证，计划准备归档。
- [2026-09-12 00:45] 完成窗口枚举稳健性修正，复验目标窗口识别及实测布局，归档计划已更新。
